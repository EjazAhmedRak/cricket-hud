{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "npm test && cargo test --manifest-path src-tauri/Cargo.toml || echo '{\"continue\":false,\"stopReason\":\"Tests failed — fix before committing.\"}'",
            "timeout": 120,
            "statusMessage": "Running tests before commit…"
          }
        ]
      }
    ]
  }
}
